HomeLife v1.3.0 handoff

Main fix: added expanded 79015 catalog and Order/Receipt Import.

Files are at ZIP root. Unzip directly into C:\Users\jason\homelife.

Deploy command:
  git add .
  git commit -m "Add expanded catalog and receipt import v1.3.0"
  git push

GitHub Pages:
  Settings > Pages > Source = GitHub Actions

Notes:
- Static deploy uses docs/index.html and docs/404.html.
- No npm install is required by the GitHub workflow.
- Price records are projection estimates and become better when receipt/order rows are merged.
- Raw receipt/order text is not persisted; only cleaned parsed rows are stored in local browser data.
